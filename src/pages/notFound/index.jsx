import { NotFoundContent } from '../../components/NotFoundContent';

/**
 * component that returns the jsx for the 404 page not found page
 */
export function NotFound() {
  return (
    <main>
      <NotFoundContent></NotFoundContent>
    </main>
  );
}
